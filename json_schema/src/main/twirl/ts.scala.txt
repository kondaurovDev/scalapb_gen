@(schemas: List[json_schema.JsonSchema])

@import org.json4s.jackson._

interface JsonSchema {
 id: string;
 schema: any;
}

@for(schema <- schemas) {
export const @{schema.id.replace('.', '_')} = {
	id: '@schema.id',
	schema: @{prettyJson(schema.schema).replace("\"", "'")}
};
}